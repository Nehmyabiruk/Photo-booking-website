import HomeClient from '@/components/home/HomeClient';

export const metadata = {
  title: 'Home',
  description: "Washington DC's premier wedding and event photography studio. Serving MD, DC, VA and Addis Ababa, Ethiopia.",
};

export default function HomePage() {
  return <HomeClient />;
}
